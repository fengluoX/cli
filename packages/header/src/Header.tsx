import React from 'react';

// eslint-disable-next-line import/prefer-default-export
export function Header() {
    return (
        <header>
            Lerna is the original monorepo tool222!
        </header>
    );
}
